/*var map;

function init(){
    map = L.map('map').setView([8.047, 34.877], 13);

    L.tileLayer('http://{s}.tile.osm.org/{z}/{x}/{y}.png', {
        attribution: '&copy; <a href="http://osm.org/copyright">OpenStreetMap</a> contributors'
    }).addTo(map);
}

init();
*/

/* var map;

function init(){
    var content = d3.select("#content");

    content.append("div")
        .attr("id", "map")
        .attr("height", "400px;")
        .attr("width", "800px;");

    map = leaflet.map('map').setView([8.047, 34.877], 13);

    leaflet.tileLayer('http://{s}.tile.osm.org/{z}/{x}/{y}.png', {
        attribution: '&copy; <a href="http://osm.org/copyright">OpenStreetMap</a> contributors'
    }).addTo(map);
}

init();
*/
